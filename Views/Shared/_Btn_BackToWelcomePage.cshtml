@model ScreeningTesterWebSite.Models.ModelBase

@{
    <p>
        @Html.ActionLink(
            linkText: "Back To Questionnaire Welcome Page",
            actionName: "WelcomePageReturn",
            controllerName: "PWSFullScreening",
            routeValues: new
            {
                coID = Model.CoID,
                loID = Model.LoID,
                envCode = Model.EnvironCode,
                languageid = Model.LanguageID,
                block = Model.EncryptedUserName,
                section = Model.EncryptedUserPW
            },
            htmlAttributes: null
                )
    </p>
}
